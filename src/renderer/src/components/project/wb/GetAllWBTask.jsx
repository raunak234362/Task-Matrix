/* eslint-disable no-unused-vars */
import { useForm } from "react-hook-form";

const GetAllWBTask = () => {
    const { register, handleSubmit, watch, setValue } = useForm();
  return (
    <div>
      
    </div>
  )
}

export default GetAllWBTask
